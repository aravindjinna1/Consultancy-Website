# Frontend Environment Variables
# Public backend API base URL (no trailing slash). Used in production (Vercel)
# so the frontend talks to the Render backend. If unset, local dev keeps the
# '/api' relative path and relies on the Vite dev-server proxy.
VITE_API_BASE_URL=https://consultancy-website-1-878m.onrender.com
